# Required for the P1 Other Teacher demo (and all subsequent AI endpoints).
ANTHROPIC_API_KEY=

# Optional — when absent, rate-limiter no-ops (useful for local dev).
# Strongly recommended in production to cap demo spend.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Later phases:
# OPENAI_API_KEY=              # P5 RAG embeddings
# RESEND_API_KEY=              # contact form (Phase 3)
# DATABASE_URL=                # P5 talk vectors (Phase 5)
# POSTHOG_KEY=                 # analytics (Phase 2+)
# CALENDLY_URL=                # booking link
